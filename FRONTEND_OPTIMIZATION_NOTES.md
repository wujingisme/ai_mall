# AI Mall 前端优化要点

## 1. 启动直接进入首页

- 原因：以前先加载登录页，再跳转，首屏慢。
- 做法：把商品首页设为启动页，会话在后台恢复。
- 效果：减少一次页面创建和跳转。

## 2. 登录后默认进入首页

- 没有返回地址：进入首页。
- 从购物车或商品详情触发登录：返回原页面。
- 导航规则统一放在 `navigation.ts`，方便以后增加页面。

## 3. 合并重复的用户校验

- 原因：应用、登录页和“我的”可能同时请求 `/auth/me`。
- 做法：多个调用共用同一个进行中的请求，成功后 60 秒内不重复校验。
- 效果：减少重复请求和等待。
- 注意：前端缓存只优化显示，后端仍必须验证 JWT 和权限。

## 4. “我的”页面先显示缓存

- 做法：先显示本地用户资料，再在后台更新。
- 效果：页面立即出现，减少空白和闪烁。
- 注意：缓存资料不能用于支付、权限等重要判断。

## 5. 首页商品短时缓存

- 30 秒内切回首页：直接显示已有商品。
- 下拉刷新、搜索、“换一批”：强制请求最新数据。
- 效果：减少重复请求，切换页面更流畅。
- 注意：下单时价格和库存必须由后端重新校验。

## 6. 商品图片懒加载

- 做法：图片增加 `lazy-load`。
- 效果：减少首屏图片请求、流量和内存压力。

## 7. 公开页面与私有页面分开

- 公开：商品首页、商品详情、“我的”游客状态。
- 私有：购物车，以及后续订单、地址、用户优惠券。
- 注意：前端守卫只改善体验，真正安全必须由后端保证。

## 面试一句话

> 我优化了小程序启动页和登录跳转，使用请求合并减少重复的用户校验；个人中心先显示缓存再后台更新；商品首页采用短时缓存和图片懒加载。缓存只用于提升浏览体验，权限、价格和库存仍由后端实时校验。

## 主要代码

- 导航：`frontend/src/utils/navigation.ts`
- 会话校验：`frontend/src/utils/session-validation.ts`
- 请求处理：`frontend/src/utils/request.ts`
- 首页缓存：`frontend/src/pages/product/list.vue`
- 个人中心：`frontend/src/pages/profile/index.vue`
