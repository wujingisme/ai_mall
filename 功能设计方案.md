# AI Mall 功能设计方案

本文集中记录后续功能的核心设计、实施批次和验收要点，便于开发、复盘和面试复习。

## 功能目录

| 编号 | 功能 | 当前阶段 |
| --- | --- | --- |
| 一 | 优惠券 | 第二批后端模板接口已完成，前端待开始 |

后续新增功能时，在目录中增加一行，并在文件末尾继续添加“功能二：XXX方案设计”“功能三：XXX方案设计”等独立章节。

---

# 功能一：优惠券方案设计

## 一、优惠券功能目标

支持管理员创建和发放优惠券，用户可以查看、领取、分享，并在订单功能完成后用于抵扣。

首版只做满减券，例如：

```text
满 100 元减 20 元
```

暂不支持折扣券、商品券、分类券和复杂叠加规则。

## 二、分批实施

| 批次 | 功能 | 状态 |
| --- | --- | --- |
| 第二批 | 优惠券模板创建、查询、启用和停用 | 后端已完成，前端待开始 |
| 第三批 | 人工发券、我的优惠券 | 已完成 |
| 第三批扩展 | 优惠券分享凭证、分享页面、领取 | 后续单独实现 |
| 第三批验收 | 两个真实微信账号完成真机分享领取 | 未开始 |
| 第四批 | 微信新用户首次登录自动发券 | 未开始 |
| 第五批 | 下单锁券、核销、取消返还和退款处理 | 等订单模块 |

## 三、核心数据

### 优惠券模板

模板用于定义一批优惠券的统一规则，本身不能直接使用。一个模板可以生成多张不同用户的优惠券。

- 名称、满减金额、使用门槛。
- 总发行量、已发行量、每人限领数量。
- 固定有效期或领取后若干天有效。
- 是否允许分享，以及草稿、启用、停用状态。

草稿可以修改；启用后核心金额、门槛和有效期规则锁定，避免已经领取的用户权益发生变化。停用只阻止继续发放，不影响用户已经获得的券。

### 用户优惠券

用户优惠券是用户实际拥有、可以在订单中使用的一张权益：

- 所属模板、用户、唯一券码。
- 未使用、已使用、已过期、已作废状态。
- 领取时间、有效期、使用时间和订单编号。

数据库必须通过唯一约束防止重复领取，并通过条件更新防止并发超发。

## 四、分享领取设计

分享链接只携带不可猜测的随机凭证：

```text
/pages/coupon/claim?shareToken=随机凭证
```

禁止在链接中放入用户编号、OpenID、优惠券码或其他敏感信息。

领取流程：

```text
用户甲创建分享
  → 后端生成随机分享凭证
  → 用户乙打开分享卡片并登录
  → 后端校验凭证、有效期、库存和限领规则
  → 给用户乙发放优惠券
```

分享记录至少包含：

- 分享凭证、优惠券模板、分享用户。
- 有效期、最大领取次数、已领取次数。
- 启用、失效或撤销状态。

## 五、安全与一致性规则

- AppSecret、OpenID 和用户编号不得出现在分享链接中。
- 分享凭证使用安全随机值，不能使用递增编号。
- 分享人默认不能领取自己的分享。
- 同一用户不能超过每人限领数量。
- 停用、过期、库存不足的优惠券不能领取。
- 重复提交同一领取请求不能产生多张意外优惠券。
- 领取资格、金额和状态全部由后端判断，不能相信前端参数。
- 并发领取依靠数据库条件更新和唯一约束共同保证。

## 六、接口规划

### 模板管理

```http
POST /api/v1/admin/coupon-templates
GET  /api/v1/admin/coupon-templates
GET  /api/v1/admin/coupon-templates/{templateId}
PUT  /api/v1/admin/coupon-templates/{templateId}
POST /api/v1/admin/coupon-templates/{templateId}/activation
POST /api/v1/admin/coupon-templates/{templateId}/deactivation
```

创建和修改金额时使用字符串金额，由后端转换为 `BigDecimal`，避免前端浮点误差。启用和停用使用独立接口，使状态变化有明确语义和权限检查。

### 人工发券与用户查询

```http
POST /api/v1/admin/coupon-grants
GET  /api/v1/me/coupons
GET  /api/v1/me/coupons/{userCouponId}
```

发券使用独立的发放记录，便于记录操作人、原因、成功和失败数量，并支持幂等和后续批量任务。

### 分享与领取

```http
POST /api/v1/me/coupon-shares
POST /api/v1/coupon-shares/resolve
POST /api/v1/coupon-claims
```

- 创建分享返回随机 `shareToken` 和小程序分享路径。
- 解析接口用于展示优惠券信息。
- 领取接口负责校验分享、库存、有效期和每人限领。
- 随机分享凭证比 JWT 更容易撤销、限制领取次数和审计，数据库只保存其哈希。

新人自动发券由后端首次创建微信用户时触发，不提供给前端重复调用。订单核销等订单模块完成后再设计，前端不能提交最终优惠金额。

接口最终名称和请求响应以实施时确认并更新后的 `api/openapi.yaml` 为准。

## 七、方案优缺点

优点：

- 模板、用户券、发放、分享和领取职责清楚。
- 便于审计、幂等、防重复领取和防超发。
- 可以继续扩展新人券、批量发放和订单核销。

缺点：

- 数据表和业务对象较多。
- 需要正确设计事务、唯一索引和并发更新。
- 大规模批量发放后续可能需要异步任务。

## 八、测试方式

本地阶段使用两个商城测试账号完成主要业务测试，不需要两个真实微信账号。

重点验证：

- 重复领取、本人领取、超过限领数量。
- 分享过期、优惠券停用、库存不足。
- 多人同时领取不会超发。
- 分享链接不泄露敏感信息。

最后使用两个不同微信账号完成一次真机分享验收。真机不能访问电脑的 `127.0.0.1`，需要可访问的 HTTPS 后端和微信合法请求域名。

## 九、面试复习要点

> 优惠券采用模板与用户实例分离设计；分享链接只携带随机凭证，所有资格由后端校验；通过唯一约束保证每人限领，通过条件更新防止并发超发；本地先用测试账号验证业务，最后再用两个真实微信账号完成分享链路验收。

核心原则：先完成模板和发放，再做分享，最后在订单模块中实现核销，避免一次性引入过多状态和风险。
